h1
  The service '#{$provider}'.'#{$name}' already exists.

a { href => url("$provider/$name?id=$hash&amp;edit") }
  Click here to edit the details

form { action => url("$provider/$name?id=$hash"), method => 'post' }
  input { type => 'hidden', name => '_method', value => 'delete' }
  or
    input { type => 'submit', value => 'Delete service' }
